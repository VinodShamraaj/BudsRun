using UnityEngine;

public class PowerUp : MonoBehaviour
{

    [SerializeField] GameObject[] powerUpList = new GameObject[0];

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
